1.1-1:成绩；
1.1-2:占用存储空间；
1.1-3:数组：连续的存储空间，使得插入、删除操作的实现比较容易，局限在于数组实现这些操作效率较低；
1.1-4:都是求路径长度的最小值；不同点在于前文的最短路不需要回到起点，而TSP问题需要回到起点；
1.1-5:找到两数公约数的最大值；工程中计算圆的周长；

1.2-1:外卖配送时，每一份订单都是一个最短路径问题；
1.2-2:实际上就是计算n<8lgn对于哪些n值成立的一个问题；n成立的最大值为5；
1.2-3:15；

思考题：
在运行时间较长情况下，时间复杂度从低到高为对数函数，根号，一次函数，一次乘对数，二次，三次，指数函数，阶乘；
